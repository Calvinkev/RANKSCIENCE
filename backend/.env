# Database (Railway auto-injects these when you link MySQL service)
# You don't need to manually set these - Railway does it automatically

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Secret - CHANGE THIS!
JWT_SECRET=your_super_secret_random_string_12345_change_me

# Frontend URL (since both are on Railway)
FRONTEND_URL=https://rankscience-production.up.railway.app