# Copy this to .env and fill values before deploying
# Database
DB_HOST=localhost
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=wunderkind_platform
DB_PORT=3306

# Server
PORT=5000
NODE_ENV=production

# JWT
JWT_SECRET=change_this_to_a_strong_random_value

# Optional: set your frontend URL for CORS if you host it separately
# FRONTEND_URL=https://your-domain.com

# Optional MySQL connection limit
# DB_CONN_LIMIT=10
